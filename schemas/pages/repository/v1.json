{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://schema.yext.com/config/pages/repository",
  "$version": "1",
  "type": "object",
  "title": "Repository",
  "description": "Configures a repository for a Yext Site.",
  "properties": {
    "$id": {
      "type": "string",
      "minLength": 1,
      "maxLength": 64
    },
    "$schema": {
      "const": "https://schema.yext.com/config/pages/repository/v1"
    },
    "repoUrl": {
      "type": "string",
      "description": "A parameter specifying the url of the repo. "
    },
    "templateRepo": {
      "type": "string",
      "description": "An optional parameter specifying the name of the template repo. The repo must be public."
    }
  },
  "required": [
    "$id",
    "$schema",
    "repoUrl"
  ]
}