{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://schema.yext.com/config/km/saved-filter",
  "$version": "1",
  "title": "Saved Filter",
  "type": "object",
  "description": "A set of filters to restrict the data returned by search.",
  "properties": {
    "$id": {
      "type": "string",
      "description": "The unique identifier for the saved filter."
    },
    "$schema": {
      "const": "https://schema.yext.com/config/km/saved-filter/v1"
    },
    "name": {
      "type": "string",
      "description": "The name of the saved filter."
    },
    "filter": {
      "type": "object",
      "description": "The details of the saved filter.",
      "additionalProperties": true
    }
  },
  "additionalProperties": false,
  "required": [
    "$id",
    "$schema",
    "name",
    "filter"
  ]
}